# Superseded — Archived

This draft was superseded and replaced by the correctly-named active plan:

[`plans/realistic-sshd-fixture-test-slice.md`](../realistic-sshd-fixture-test-slice.md)

The active plan was subsequently revised to add:
- Phase 1: Agent SSH Identity Service
- Phase 2: `get_agent_public_key` MCP tool

before the original sshd fixture content (now Phase 3).
