# Moved to archive

This plan has been archived. See [`plans/archive/node-management-api-slice.md`](archive/node-management-api-slice.md).

The implementation is complete. The live codebase uses `get_node_status` (not the older `get_status` referenced in early versions of this document).
